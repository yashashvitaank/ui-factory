import Loader from "@/components/Loader";

function Loading() {
  return (
    <div
      style={{
        width: "100%",
        height: "100dvh",
        display: "grid",
        placeItems: "center",
        position: 'fixed'
      }}
    >
      {/* <Loader /> */}
    </div>
  );
}

export default Loading;
